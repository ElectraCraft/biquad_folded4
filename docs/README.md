# docs/

Supporting artifacts from the signed-off LibreLane run (`hd_lib_60ns`).

- `results/` — raw signoff reports: metrics.csv, DRC (Magic + KLayout), LVS (Netgen), worst-corner timing
- `screenshots/` — OpenROAD GUI captures across floorplan, placement, and routing stages
- `gds/` — final GDSII layout
